cookbooks:
- aws:
- build-essential:
- openssl:
- mysql:
- postgresql:
- xfs:
- database:
- apps:
- apps-database:
- chef_handler:
- minitest-handler:

environments:
- production:

roles:
- standalone:

data bags:
- apps:
  - princess
  - toad
  - www

nodes:
- ec2 1:
  - role[standalone]
  - --no-host-key-verify -x ubuntu -G default -I ami-3c994355 -f c1.medium -d chef-full --region us-east-1 --environment production
