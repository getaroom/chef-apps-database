cookbooks:
- aws:
- build-essential:
- openssl:
- mysql:
- postgresql:
- xfs:
- database:
- apps:
- apps-database:
- chef_handler:
- minitest-handler:

nodes:
- ec2 1:
  - recipe[minitest-handler] recipe[apps-database]
  - --no-host-key-verify -x ubuntu -G default -I ami-3c994355 -f c1.medium -d chef-full --region us-east-1
